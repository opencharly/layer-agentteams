# 2026.238.2102 — fix: advance bare-name candy refs to standalone repos (Phase-4 residue)

## Summary

Phase-4 candy de-submodule cutover residue: this manifest's `candy:`/`require:` list
referenced 5 candy(s) by BARE name (agentteams-controller, agentteams-element, agentteams-higress) — the pre-cutover same-repo
sibling form. Post-cutover each moved candy lives in its own standalone repo, so the
bare name fails resolution (`unknown candy "agentteams-controller"` — surfaced by the distro
box sweep's class gate).

Fix: advance every bare-name ref to its standalone
`@github.com/opencharly/<kind>-<name>:v<tag>` ref (the cutover end-state).

## How tested (final committed tree, live)

**1. The changed refs (raw diff additions):**
```
+            - '@github.com/opencharly/pod-agentteams-minio:v2026.237.454'
+            - '@github.com/opencharly/pod-agentteams-matrix:v2026.237.454'
+            - '@github.com/opencharly/pod-agentteams-element:v2026.237.454'
+            - '@github.com/opencharly/pod-agentteams-higress:v2026.237.454'
+            - '@github.com/opencharly/pod-agentteams-controller:v2026.237.454'
```

**2. Fresh-resolve gate — the OLD manifest fails with THIS repo's own unknown-candy
errors; the FIXED manifest clears them:**
```
$ # OLD manifest (main): this repo's bare refs fail resolution
$ /tmp/charly-phase4/bin/charly box validate
candy "github.com/opencharly/layer-agentteams" candy: unknown candy "agentteams-minio"
candy "github.com/opencharly/layer-agentteams" candy: unknown candy "agentteams-matrix"
candy "github.com/opencharly/layer-agentteams" candy: unknown candy "agentteams-element"
candy "github.com/opencharly/layer-agentteams" candy: unknown candy "agentteams-higress"

$ # FIXED manifest (this PR) via CHARLY_REPO_OVERRIDE — real output below:
$ CHARLY_REPO_OVERRIDE="github.com/opencharly/layer-agentteams=/tmp/verify-layer-agentteams" \
    /tmp/charly-phase4/bin/charly box validate; echo "exit=$?"
(no stderr — clean)
```
**Exit code: 0 — the gate passes clean (no errors, no warnings).**

**3. The new refs resolve against the live remote (tags exist):**
```
cf567d465423f1644c0fdb8a066419b766488b95  pod-agentteams-controller  v2026.237.454
23c5447763d4ae33ecdd3b21143e353072bf43d6  pod-agentteams-element  v2026.237.454
12db34f0106ec7a1e4cadfcdeaaefbd6cb75f6d8  pod-agentteams-higress  v2026.237.454
5420d16788a1186269f4b68b5023c54a9fb70e40  pod-agentteams-matrix  v2026.237.454
322be377bc30dcdd15696b4e99268c5f63a4e0b0  pod-agentteams-minio  v2026.237.454
```

## Rulebook compliance

| Rule | Status |
|---|---|
| R0 skills first | git-workflow loaded; the fix follows the Phase-4 sweep pattern |
| R1 RCA | bare-name deps are cutover residue — sibling-dep qualification only resolves same-repo deps; the standalone form is the end-state |
| R2 no deferral | the residue is fixed in-tree in this PR — no follow-up needed for this repo's refs (sibling repos' residue is routed to their own PRs in this same sweep) |
| R3 one canonical implementation | the refs now use the same @github standalone-ref form as every other cross-repo dep |
| R4 no workarounds | the deps advance to real standalone repos/tags; no local hack |
| R5 hard cutover | the in-repo bare-name path is gone (the diff removes it) |
| R6 git safety | clean status before branch; no force-push; PR-only |
| R7 prove the gate | pasted fresh-resolve gate above with real output and exit code |
| R8/R9 | N/A — no image build / deployment in this config-only change |
| R10 fresh proof | the fresh-resolve gate ran live on the final committed tree (pasted); the identical ref shapes are additionally gated by charly's TestCandyCutoverSweep_AllRemotePinsResolve |

## Change classification

- Change class: config fix (bare-name candy refs → standalone @refs)
- Verification gate: fresh-resolve gate (pasted, OLD-fails/FIXED-clears) + org pr-validator on this PR
- Attribution tier: `analysed on a live system` — the changed resolution path ran live
  (the box validate gate exercised the fixed manifest, output pasted); a full box
  rebuild was not run (no KVM bed in this repo's CI)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*